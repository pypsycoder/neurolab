# НейроЛаб — дорожная карта

**Версия:** 0.1  
**Создана:** 2026-09-13  
**Назначение:** от проверяемых интеграций GigaChat, gpt2giga, Hermes,
OpenHands, LangGraph и MCP — к безопасной клинической системе поддержки
пациентов на гемодиализе.

## 1. Цель, архитектура и границы

НейроЛаб — инженерно-исследовательская среда, которая исследует агентные
системы и решения для помощи пациентам на гемодиализе, помогает разрабатывать
код и превращает только проверенные компоненты в отдельный пациентский продукт.

```text
Исследовательско-инженерный контур
GigaChat/gpt2giga → LangGraph Supervisor → Hermes / OpenHands / MCP-инструменты
                                      ↘ репозиторий, тесты, изолированные worktree

Клинический контур
Пациент/врач → API и клинический workflow → ограниченная модель/база знаний
                                      ↘ аудит, мониторинг, эскалация специалисту
```

Пациентский контур не получает shell, браузер, Git, репозиторий, произвольный
MCP или право менять код. Он не ставит диагноз, не назначает лечение и не
заменяет врача. При опасных симптомах он следует утверждённому маршруту
эскалации, а не пытается дать медицинское заключение.

## 2. Общие принципы готовности

- Каждый этап имеет измеримый результат, тесты и явное решение `go / no-go`.
- До клинической валидации используются синтетические, обезличенные и законно
  разрешённые данные; секреты и идентификаторы пациентов в репозиторий не
  попадают.
- Медицинские утверждения имеют первоисточник, дату проверки, клинического
  владельца, историю изменения и срок пересмотра.
- Модель не является источником истины: пороги, протоколы и маршрутизация
  хранятся в версионируемых правилах и утверждённой базе знаний.
- Агенты работают с минимальными правами, ограничением времени/затрат/циклов,
  аудитом и возможностью немедленной остановки человеком.
- Технический успех сам по себе не разрешает работу с пациентами: требуются
  клиническое, правовое, ИБ- и эксплуатационное одобрения.

## 3. Этапы

### Этап 0. Инициация и контроль проекта

**Цель:** подготовить воспроизводимую среду и правила принятия решений.

1. Назначить владельцев: продукт, клиническая безопасность, ИБ/данные,
   инженерия и эксплуатация.
2. Зафиксировать разрешённые сценарии и запреты для исследовательского,
   инженерного, врачебного и пациентского контуров.
3. Подготовить репозиторий, CI, шаблоны ADR, реестр рисков, трекер решений,
   политику секретов и раздельные окружения `dev/test/prod`.
4. С юристом, DPO и ИБ определить применимые правила персональных данных,
   медицинского ПО и клинической ответственности для целевой юрисдикции.

**Критерии готовности:** владельцы и границы утверждены; есть реестр рисков,
изолированные окружения и секреты вне репозитория.

### Этап 1. Технические интеграционные пробы

**Цель:** доказать работоспособность компонентов по отдельности и в простой
сквозной цепочке, без пациентских данных.

#### 1.1. GigaChat

Проверить аутентификацию, доступные модели, лимиты, потоковые ответы, ошибки,
квоты/стоимость и безопасное журналирование. Добавить contract- и
negative-тесты для таймаутов, неверной авторизации и исчерпания лимита.

#### 1.2. gpt2giga

Развернуть адаптер в изолированном окружении. Сверить совместимость форматов
запросов, structured output и tool calls (если поддерживаются), повторы,
таймауты и безопасную деградацию при сбое провайдера.

#### 1.3. Hermes

Выбрать конкретную версию/проект и определить его роль: исследователь, а не
клинический валидатор. На открытых источниках проверить поиск, сбор ссылок,
извлечение тезисов, дат, ограничений и степени уверенности.

#### 1.4. OpenHands

Запустить в одноразовом sandbox/worktree с минимальными правами. Выполнить
задачу «исправить маленькую ошибку → запустить тесты → сформировать diff».
Запретить доступ к продакшен-ключам, произвольной сети и учётным данным.

#### 1.5. LangGraph

Собрать минимальный граф:
`задача → планировщик → один агент/инструмент → валидатор → отчёт`.
Проверить сохранение состояния, checkpoint, возобновление после сбоя, лимит
итераций, отмену и трассировку.

#### 1.6. MCP

Подключить только тестовые серверы: файловый, документация/поиск и Git в
тестовом репозитории. Для каждого инструмента описать контракт, схему
параметров, разрешения и негативные тесты: отказ доступа, ошибочный вызов,
prompt injection из недоверенного результата.

#### 1.7. Сквозной smoke test

Выполнить сценарий: «найти источники → спланировать изменение учебного кода →
внести изменение в worktree → запустить тесты → вернуть отчёт с ссылками и
diff». Артефакт должен быть повторяемым.

**Критерии готовности:** автоматические smoke/integration-тесты зелёные;
права и секреты изолированы; у каждой интеграции есть trace, отчёт и безопасный
отказ.

### Этап 2. Контролируемый исследовательский и инженерный контур

**Цель:** сделать агентов полезными, но управляемыми человеком.

1. Разделить роли LangGraph: supervisor, исследователь, разработчик,
   тестировщик, reviewer, synthesizer. Для каждой установить допустимые
   инструменты, лимиты циклов, времени и затрат.
2. Создать research pipeline: поиск → первоисточники → извлечение цитат и
   ограничений → сопоставление источников → отчёт с пробелами и датой проверки.
   Хранить промежуточные артефакты; не выдавать неподтверждённый клинический
   вывод за факт.
3. Создать engineering pipeline: задача → план → worktree → правки → format/
   lint/type-check → unit/integration/security tests → review → ручное
   утверждение merge. Агент не сливает код и не деплоит prod сам.
4. Защититься от инъекций и опасных вызовов: веб-страницы и документы — это
   недоверенные данные, параметры MCP валидируются схемой и политикой.
5. Ввести наблюдаемость: ID запуска, версии модели/промпта/графа/инструмента,
   расходы, latency, ошибки, решение валидатора и ссылку на артефакты.

**Критерии готовности:** эталонный набор исследовательских и кодовых задач
достигает заданных метрик; опасные вызовы блокируются; у каждой правки есть
цепочка «задача → diff → тесты → review».

### Этап 3. Клиническая постановка и база знаний

**Цель:** выбрать ограниченный и проверяемый первый клинический сценарий.

1. Совместно с нефрологами выбрать один узкий use case: например, объяснение
   утверждённой памятки, подготовка вопросов к диализу или навигация по
   процедуре. Не начинать с диагностики, терапии или дозировок.
2. Утвердить контент-процесс: источники, авторы, рецензенты, дата редакции,
   уровень доказательств, аудит и срок пересмотра.
3. Описать клинические границы и красные флаги. Для каждого риска нужны
   утверждённый текст, маршрут эскалации, владелец и тест-кейс.
4. Построить версионируемую RAG-базу только из разрешённого корпуса — с
   ссылками на фрагменты, контролем доступа и отказом отвечать вне покрытия.
5. Создать синтетический safety-набор: типовые вопросы, неполные данные,
   красные флаги, конфликтующие инструкции, инъекции, недопустимые запросы и
   языковые варианты.

**Критерии готовности:** клинический владелец письменно утвердил use case,
контент, границы и эскалации; ответы прослеживаются до источников; красные
флаги покрыты автоматическими тестами.

### Этап 4. Неавтономный клинический прототип

**Цель:** создать отдельный прототип для закрытого внутреннего испытания.

1. Реализовать отдельный API/UI пациентского помощника без доступа к
   исследовательскому и инженерному контурам.
2. Реализовать согласия, минимизацию данных, RBAC, шифрование, сроки хранения,
   экспорт/удаление по применимым правилам и неизменяемый аудит.
3. Реализовать workflow: намерение → проверка покрытия знаниями → ответ по
   утверждённому контенту → safety-проверка → эскалация/отказ. При низкой
   уверенности система не угадывает.
4. Создать clinician review: просмотр и разметка диалогов, корректировка
   контента, обратная связь без скрытого обучения на данных пациентов.
5. Провести threat model, анализ вреда, privacy/security, нагрузочные,
   accessibility и usability-тесты с нужными согласиями.

**Критерии готовности:** safety-набор пройден; критические сценарии верно
эскалируются; нет пути к инженерным инструментам или данным вне полномочий.

### Этап 5. Клиническая валидация и готовность к пилоту

**Цель:** доказать безопасность и полезность в заявленном назначении.

1. Зафиксировать intended use, нецелевые применения, KPI, критерии остановки и
   план клинической оценки.
2. Провести независимую ретроспективную оценку на утверждённых обезличенных
   или синтетических данных: точность, полнота эскалаций, ложные успокоения,
   опасные отказы и справедливость по группам.
3. Провести проспективное внутреннее испытание с наблюдением специалистов; не
   давать системе самостоятельных лечебных полномочий.
4. Получить юридическую, этическую, ИБ- и регуляторную оценку для конкретной
   юрисдикции; установить применимость требований к медицинскому ПО.
5. Утвердить инструкции, обучение, SLO, поддержку, резервное восстановление,
   change control, rollback и постмаркетинговый мониторинг.

**Go/no-go:** пилот возможен только при формальном одобрении клинического
владельца, ИБ, юриста/регуляторного владельца и эксплуатации, без незакрытых
критических рисков.

### Этап 6. Ограниченный клинический пилот

**Цель:** проверить систему в реальном процессе под постоянным контролем.

1. Ограничить когорту, учреждения и сценарии; включить feature flags, kill
   switch и ручную эскалацию.
2. Регулярно анализировать качество, инциденты, эскалации, отказы, дрейф,
   нагрузку и обратную связь пациентов и врачей.
3. Все изменения моделей, промптов, контента, workflow и интеграций проводить
   через оценку риска, регрессионные тесты, клиническое согласование и версии.

**Критерии готовности:** достигнуты заранее утверждённые safety/usefulness
метрики, инциденты укладываются в SLA, независимый обзор рекомендует
масштабирование либо обосновывает остановку.

### Этап 7. Полноценная эксплуатация и улучшение

**Цель:** масштабировать подтверждённые сценарии без ослабления контроля.

1. Расширять учреждения, языки и сценарии только после самостоятельной
   валидации каждого изменения.
2. Поддерживать мониторинг, аудит, управление уязвимостями, переаттестацию
   доступов, резервное восстановление и учения инцидентов.
3. Регулярно переоценивать клинические знания, риски вреда, справедливость и
   регуляторную применимость, а также после существенного изменения модели.
4. Сохранять разделение контуров: код от Dev Agent проходит review, CI,
   security и клиническую валидацию до пациентского контура.

**Критерии готовности:** подтверждённое соответствие заявленному назначению,
прослеживаемость решений и устойчивая эксплуатационная модель.

## 4. Сквозные тесты и обязательные артефакты

| Область | Что проверять | Обязательный артефакт |
|---|---|---|
| Интеграции | контракт, таймауты, отказ, повтор, лимиты | integration tests и trace запуска |
| Агентный граф | checkpoint, отмена, предел итераций, policy | тесты состояний и audit trail |
| Код-агент | изоляция, diff, CI, секреты, опасные команды | worktree, review, результаты CI |
| Исследования | первоисточник, дата, цитаты, конфликт источников | отчёт со ссылками и уверенностью |
| Клиническая безопасность | красные флаги, отказы, эскалации, вред | утверждённый safety-набор и review |
| Данные и ИБ | доступ, шифрование, аудит, удаление, инъекции | threat model и security assessment |
| Эксплуатация | нагрузка, мониторинг, rollback, инциденты | runbook, SLO, учение восстановления |

## 5. Стоп-условия

Остановить автоматическое действие, изолировать релиз и передать владельцу
риска при: утечке/несанкционированном доступе к данным; опасном пациентском
совете или пропущенном красном флаге; выходе агента за полномочия;
неподтверждённом клиническом контенте; обходе аудита; критической уязвимости;
невозможности откатить версию. Возобновление допускается только после
документированной причины, исправления, регрессионной проверки и одобрения.

## 6. Журнал выполнения

После каждого завершённого этапа или существенного подэтапа добавлять строку.
Фиксировать факт, а не план; не удалять старые записи.

| Дата (YYYY-MM-DD) | Этап / подэтап | Статус | Фактический результат | Тесты / доказательства | Проблемы и решения | Следующий шаг |
|---|---|---|---|---|---|---|
| 2026-09-13 | 0 — планирование | выполнен | Созданы ROADMAP.md и правила ведения для агентов в AGENTS.md. | Файлы созданы и проверены вручную. | AGENTS.md был read-only синхронизированным файлом; атрибут снят для внесения требуемых правил. | Назначить владельцев и подготовить окружение этапа 0. |
| 2026-09-13 | 0.2–0.3 — базовое управление и границы | выполнен | Созданы реестр ролей и решений, границы контуров, стартовый реестр рисков, политика окружений/секретов и шаблон ADR. | Ручная проверка: документы содержат роли, доступы, запреты, риски, dev/test/prod и шаблон решений. | Конкретные владельцы, юрисдикция и секретное хранилище не могут быть назначены без решения пользователя/организации; оставлены как открытые решения. | Утвердить владельцев и юрисдикцию; затем начать этап 1.1 с безопасной конфигурации GigaChat. |
| 2026-09-13 | 0.1 и 0.4 — владельцы и применимые требования | в работе | В `PROJECT_GOVERNANCE.md` созданы роли, обязательные решения и перечень предварительных решений для клинического контура. | Проверены наличие всех шести ролей и пяти обязательных решений. | Нельзя назначить людей, организацию-оператора и юрисдикцию предположением; до этого клинический контур не запускается. | Получить назначения и целевую юрисдикцию; после этого оформить ADR-0001 и завершить этап 0. |
| 2026-09-13 | 0.1 и 0.4 — исследовательский режим РФ | выполнен | Владелец проекта назначен временно ответственным за не-клинический режим; РФ выбрана рабочей юрисдикцией; принят ADR-0001: только публичные и синтетические данные, без пациентов, clinical/patient/prod контуров. | Перекрёстная проверка ADR-0001, управления, границ, рисков и политики окружений. | Полная правовая/регуляторная оценка и клинический владелец отложены до изменения режима; это явно запрещает переход к реальным данным или пациентскому продукту. | Начать 1.1: уточнить актуальный контракт GigaChat и создать безопасный integration-test scaffold без ключей. |
| 2026-09-13 | 1.1 — GigaChat, безопасный каркас | в работе | Выбран Python 3.11 и официальный SDK `gigachat` 0.2.3; созданы конфигурация, фабрика клиента, `.env.example`, unit- и opt-in live-тесты. Зафиксированы актуальные endpoint `https://api.giga.chat/v1` и TLS-политика. | В `.venv` установлены зависимости. После корректировки теста: 5 unit-тестов пройдены, 1 live-тест пропущен без явного `RUN_GIGACHAT_LIVE=1`; сигнатура SDK сверена с параметрами адаптера. | SDK не публикует `base_url` как открытый атрибут; проверка заменена на создание/закрытие клиента, а передаваемые параметры проверяются fake-клиентом. Ключ авторизации отсутствует, поэтому аутентификация, список моделей, лимиты и реальные ответы ещё не проверены. | Передать ключ только локально через `GIGACHAT_CREDENTIALS`, включить live-тест и зафиксировать результат. |
| 2026-09-13 | 1.1 — перенос выполнения на RP5 | заблокирован | Принят ADR-0002: GitHub — источник кода, Raspberry Pi 5 — `dev/test` среда через SSH/Tailscale; секреты остаются на Pi и игнорируются Git. GitHub-плагин подтвердил аккаунт `pypsycoder`; на RP5 подтверждены доступность и Raspberry Pi Connect. | Проверены существование `.gitignore`, доступность RP5 в Connect и отсутствие найденного репозитория `psycoder` в подключённом GitHub-аккаунте. | GitHub не разрешает этому аккаунту private repositories и предлагает только public. Публикация без явного решения пользователя не выполнена. SSH-конфигурация локально недоступна, а Tailscale hostname не указан. | Получить решение о публичности `pypsycoder/psycoder` либо иной Git-хостинг; затем получить SSH/Tailscale endpoint RP5, клонировать репозиторий и провести тесты на Pi. |
| 2026-09-13 | 1.1 — GitHub bootstrap | выполнен | Созданный пользователем публичный `pypsycoder/neurolab` заполнен стартовым каркасом: код, 6 тестов, документация, AGENTS/ROADMAP и `.gitignore`; отправлен commit `7fd7c7b` в `main`. | Проверен staging: 21 публикуемый файл, без `sources/`, `.venv`, локальных `.env` и ключевых файлов. `git push -u origin main` завершён успешно. | GitHub-плагин не имеет права записи в этот репозиторий (403), поэтому использован локальный Git как разовый безопасный bootstrap. RP5 пока не подключён по SSH/Tailscale. | Получить SSH/Tailscale endpoint RP5; клонировать `https://github.com/pypsycoder/neurolab.git`, настроить локальный `.env`, установить зависимости и выполнить тесты на Pi. |
| 2026-09-13 | 1.1 — подготовка запуска на RP5 | выполнен | Добавлены `scripts/bootstrap_rp5.sh` (изоляция, зависимости, unit-тесты) и `scripts/verify_gigachat_live.sh` (opt-in live-тест с локальным `.env`); README дополнен безопасным порядком запуска на Pi. | 5 unit-тестов прошли, 1 live-тест ожидаемо пропущен без ключа. Локальная проверка Bash недоступна из-за отказа доступа Windows Bash Service; синтаксис будет проверен на RP5. | Нужен SSH/Tailscale endpoint RP5 для клонирования, Bash-проверки, установки зависимостей и живой аутентификационной проверки GigaChat. | Получить endpoint RP5 и развернуть `pypsycoder/neurolab` на Pi. |
| 2026-09-13 | 1.1 — обнаружение Tailscale/RP5 | выполнен | В других проектных чатах подтверждены Tailscale/SSH-доступ и существующая среда RP5. Созданы публичная безопасная инструкция `docs/deployment/RP5.md` и локальная исключённая из Git заметка `docs/private/RP5_ACCESS.md`. | Сверены прежние инвентаризация, Tailscale DNS/IP, ответ TCP/22 и ключевой SSH-доступ; публичный документ не содержит endpoint, IP, путь к ключу или значения секретов. | На Pi уже существует отдельный `/opt/neuro-lab` и `sing-box`; новый репозиторий должен оставаться изолированным в `/home/bimo/neurolab`. | Выполнить строго проверяемое SSH-подключение, клонировать репозиторий на RP5 и запустить bootstrap/test. |
| 2026-09-13 | 1.1 — уточнение RP5 topology | выполнен | Исправлена архитектурная ошибка: существующий `/opt/neuro-lab` — основной стек НейроЛаба и целевая рабочая копия для связи с `pypsycoder/neurolab`; `sing-box` остаётся отдельным сервисом. | Пользователь явно подтвердил назначение существующего стека; `docs/deployment/RP5.md`, локальная access-запись и ADR-0002 синхронизированы. | Историю и состояние существующей рабочей копии нельзя заменять вслепую; сначала требуется read-only Git/Compose инвентаризация. | Подключиться по SSH, зафиксировать Git/Compose state и спланировать безопасное добавление `origin`. |
| 2026-09-13 | 1.1 — привязка действующего стека RP5 к GitHub | в работе | На `/opt/neuro-lab` подтверждены Compose-стек из шести сервисов и отсутствие прежней Git-истории. Создана ветка-снимок `rp5-control-plane`, добавлен SSH `origin`; 23 проверенных файла зафиксированы коммитом `450f41e` и опубликованы в GitHub. Для репозитория создан отдельный Ed25519 deploy key на Pi; приватная часть имеет режим `0600` и не покидала устройство. Работающие контейнеры и `.env` не изменялись. | Tailscale/SSH; `git ls-remote`, `git push -u origin rp5-control-plane` и сверка remote SHA подтвердили публикацию; `git check-ignore -v .env` и проверка индекса подтвердили исключение `.env`; `git status --short --branch` чист. Сравнение деревьев с `origin/main` выявило лишь три пересекающихся пути: `.env.example`, `.gitignore`, `README.md`. | У ветки-снимка и `main` независимые истории; автоматическое слияние без проверки создаст конфликты в трёх общих файлах. | В ветке RP5 выполнить контролируемое объединение `main`, вручную сохранить безопасные правила `.env`/`.gitignore` и актуальное описание Compose-стека, затем запустить доступные проверки до отдельного merge-коммита. |
| 2026-09-14 | 1.1 — объединение RP5-стека и безопасная live-проверка | в работе | В `rp5-control-plane` начато контролируемое слияние `main`: три конфликта разрешены без изменения `.env`; в `.gitignore` сохранены исключения `.env`, runtime и локальных заметок; из публичного README удалён конкретный Tailnet-адрес. Скрипт live-проверки изменён: `.env` разбирается как данные через `python-dotenv`, а не исполняется shell. | `bash -n` для обоих скриптов; 5 unit-тестов прошли, 1 live-тест ожидаемо skipped без opt-in; все шесть Compose-сервисов продолжают работать. Безопасный opt-in live-test с уже локальным ключом дошёл до TLS-подключения и был отклонён из-за self-signed certificate chain, без вывода значения ключа. | Предыдущая ручная попытка `source .env` обнаружила несовместимый с shell формат файла и могла вывести фрагмент стороннего ключа в технический лог. Больше `.env` не исполняется; затронутый сторонний ключ нужно отозвать/перевыпустить. TLS не отключался. | Зафиксировать и отправить merge-коммит; отдельно подтвердить источник и fingerprint доверенного CA для сетевого контура, установить его штатно только после проверки и повторить live-test. |
| 2026-09-14 | 1.1 — интеграция GitHub и RP5 | выполнен | Рабочий стек в `/opt/neuro-lab` связан с GitHub по репозиторному SSH deploy key. История объединена с `main` с сохранением актуального дерева RP5, документации и безопасного live-test runner; публичные документы не содержат приватный Tailnet endpoint. | `git push` и сверка remote refs; `bash -n`; 5 unit-тестов passed, 1 opt-in live-test skipped; Compose: 6 сервисов running, PostgreSQL и Redis healthy; `.env` и `docs/private/` подтверждённо игнорируются. | Рабочий GigaChat live-test блокируется корректной TLS-проверкой из-за self-signed certificate chain. Нельзя обходить её флагом отключения SSL; требуется проверенный доверенный CA. | Отозвать/перевыпустить сторонний ключ, который мог попасть в технический вывод; подтвердить источник и fingerprint CA, штатно установить его в доверенное хранилище RP5, затем повторить live-test и зафиксировать результат. |
| 2026-09-14 | 1.1 — диагностика TLS GigaChat | выполнен | Официальная страница Госуслуг подтвердила актуальные файлы НУЦ Минцифры. Корень из цепочки GigaChat совпал по SHA-256 с официальным `Russian Trusted Root CA`; root и два выпускающих сертификата штатно установлены в CA store RP5. Live-runner передаёт системный CA-bundle только тестовому процессу, TLS не отключается. | `openssl s_client -verify_return_error`: Verification OK; безопасный opt-in запрос списка моделей прошёл (`1 test OK`); Compose: 6 сервисов running, PostgreSQL и Redis healthy. | SDK использует `certifi`, поэтому после системной установки требовалось явно передать `SSL_CERT_FILE` для одного тестового процесса. Скрипт не исполняет `.env` как shell. | Отозвать/перевыпустить сторонний ключ, который мог попасть в ранний технический вывод; затем зафиксировать закрытие 1.1 и перейти к контракту gpt2giga. |
| 2026-09-14 | 1.2 — gpt2giga, изолированная проверка | выполнен | На RP5 из pinned `ai-forever/gpt2giga` v0.3.0 (commit `197ef42`) собран отдельный loopback-only evaluation-контур. Успешно проверены health, OpenAI-compatible model discovery и синтетический chat request. После теста отдельные container/network остановлены и удалены; основной стек не перезапускался. | `GET /health` 200; `GET /v1/models` 200, 10 моделей; `POST /v1/chat/completions` 200 со структурой `choices`, `usage` и метаданными. Конфигурация `0600`; runtime и секреты игнорируются Git. | Upstream Compose по умолчанию использует `latest`; для проверки собраны зафиксированные исходники v0.3.0. Для TLS container получил read-only system CA-bundle. Streaming, structured output, tool calls, timeout/retry и upstream failure пока не оценены. | Начать 1.3: выбрать и проверить Hermes как исключительно исследовательский агент на открытых источниках. |
| 2026-09-14 | 1.3 — Hermes, изолированная связность и no-go для поиска | в работе | Зафиксирован Hermes Agent `v2026.9.11` (`939e45c`), развёрнут только в игнорируемом `runtime/` и подключён через временный loopback-only gpt2giga. Синтетический запрос без инструментов прошёл; policy программно подтвердила `tools=0` и отключённую память. Отдельный search-only профиль не допущен: зарегистрированных вызовов `web_search` нет. | `hermes --safe-mode` вернул `HERMES_OK`; проверка конфигурации подтвердила loopback provider, отключённую память и пустой CLI toolset. Для search-only profile фактический session trace: `tool_call_count=0`; повтор с явным требованием вызова также не дал tool trace. Основной Compose после targeted cleanup: 6 сервисов running, PostgreSQL/Redis healthy. | Hermes по умолчанию добавлял `kanban` даже при пустом профиле; он явно запрещён и повторная policy-проверка зелёная. В поисковой пробе зафиксирован rate limit upstream, а ответ модели без trace нельзя считать поиском. | Не включать Hermes как исследовательский агент. Сначала проверить в gpt2giga OpenAI tool calls, trace и честную деградацию/повторы при 429; затем повторить search smoke-test с одной разрешённой операцией. |
| 2026-09-14 | 1.2/1.3 — офлайн-контракт tool calls | выполнен | Для pinned gpt2giga создано отдельное ignored test-окружение. Подтверждены преобразование tool definitions, OpenAI golden fixture и hermetic bridge OpenAI/Anthropic/Gemini без рабочих credentials. | `tests/test_utils/test_convert_tools.py`: 33 passed при запрете сокетов; повторный набор из tool fixture и `test_openai_compatible_protocol_bridge.py`: 43 passed, 1 warning. | Полный запрет сокетов блокирует локальные event-loop/ASGI механизмы protocol-тестов, поэтому они запускались с пустым окружением, инертными credentials и upstream test doubles. Это не заменяет живой trace Hermes. | Дождаться доступного лимита, добавить ограниченный retry/backoff при 429 и повторить единственный live `web_search` с проверкой tool trace; до этого Hermes остаётся no-go для поиска. |
| 2026-09-14 | 1.3 — повторный live search smoke-test | заблокирован | Выполнена ровно одна повторная попытка search-only Hermes после успешных офлайн-контрактов; обязательный tool trace не создан. Временный proxy остановлен, основной Compose не изменялся. | По последней сессии `tool_call_count=0`; Hermes не завершил запрос за лимит времени. | После остановки proxy временный Hermes-процесс завершился по timeout; повторов не выполнялось, чтобы не расходовать лимит провайдера и не принять текст модели за поиск. | Не включать Hermes для поиска. Нужна диагностика зависания на уровне Hermes/gpt2giga с локальным mock upstream и явный trace одного `web_search`; затем отдельный live test при доступном лимите. |
| 2026-09-14 | 1.3 — локальная диагностика streaming mock | в работе | Временный loopback-only mock отделил Hermes от gpt2giga/GigaChat и внешней сети. Обычный JSON-ответ был отклонён как пустой stream; после перехода на SSE Hermes всё ещё не принял tool call, trace остался нулевым. Mock остановлен. | `tool_call_count=0`; зафиксированы ошибки `empty stream` и затем `empty content`; основной Compose не изменялся. | Для диагностики нужен точный OpenAI streaming delta-формат (`choices[].delta.tool_calls`), а не полное chat-completion JSON в SSE. | Создать минимальный корректный SSE tool-call fixture из тестов Hermes, подтвердить один `web_search` и trace локально; live-поиск остаётся запрещён. |
| 2026-09-14 | 1.3 — локальный полный цикл Hermes search | выполнен | Loopback-only mock с корректными OpenAI `chat.completion.chunk` и `delta.tool_calls` подтвердил полный цикл: один `web_search` → синтетический результат SearXNG → финальный отчёт Hermes. Mock остановлен. | Последняя сессия: `tool_call_count=1`; финальный отчёт содержит URL, название, дату, тезис, ограничение и уверенность. | Первые mock-версии возвращали JSON вместо stream и буквальные `\\n` вместо SSE-разделителей; оба дефекта устранены. Это доказывает Hermes policy/trace, но не живую связку с gpt2giga/GigaChat. | Сохранить Hermes как локально подтверждённый research-runner; повторить один live trace только после восстановления лимита провайдера, затем переходить к OpenHands. |
| 2026-09-14 | 1.3 — live search после подтверждения доступности токенов | заблокирован | Один live запуск gpt2giga/GigaChat завершился без `429` и вернул текстовый ответ, но не выполнил обязательный `web_search`; временный proxy остановлен. | Ответ сформирован, однако session trace: `tool_call_count=0`; основной Compose не изменялся. | Лимит провайдера доступен; текущая модель/bridge не соблюдает требование tool call и возвращает неподтверждённый текст. | Не включать live Hermes search. Проверить модель/настройки gpt2giga для обязательного `tool_choice`, либо использовать локально подтверждённый mock contract только как регрессионный тест. |
| 2026-09-14 | 1.3 — диагностика обязательного tool call | выполнен | Pinned gpt2giga подтверждённо преобразует явный OpenAI `tool_choice` в обязательный function call GigaChat; Hermes передаёт `tools`, но не `tool_choice`, поэтому его prompt-level enforcement не гарантирует поиск. Принят ADR-0003: ответ без search trace — отказ, обязательный поиск будет отдельным программным узлом будущего графа. | Два upstream contract-теста gpt2giga: `2 passed`; исходники Hermes: `ChatCompletionsTransport._base_kwargs` добавляет `tools`, но не `tool_choice`; ранее локальный SSE mock: `tool_call_count=1`, live trace: `0`. | Нельзя безопасно исправить гарантию только текстом prompt или незакоммиченным патчем Hermes runtime. Решение переносит обязательность в контролируемый, версионируемый workflow. | Перейти к 1.4: проверить OpenHands в изолированном одноразовом worktree без секретов и без доступа к основному Compose. |
| 2026-09-14 | 1.4 — OpenHands, изолированный Dev Agent | выполнен | Проверен OpenHands CLI `1.16.0` ARM64 с SHA-256 release digest. В одноразовом Git-worktree headless agent через временный gpt2giga исправил синтетическую ошибку `add`; затронут ровно один файл. Временный proxy и одноразовый ключ удалены. | CLI/SDK version check; SHA-256 совпал с GitHub release digest; agent exit code `0`; `python3 -m unittest discover -s openhands_eval_fixture`: `1` test passed; diff только `openhands_eval_fixture/calculator.py`; основной Compose: 6 сервисов running. | Headless всегда auto-approve, поэтому запуск разрешён только в Docker без socket, с ограниченными монтированиями и внутренней сетью. Docker сообщил, что memory cgroup limits на RP5 недоступны; этот лимит не считается подтверждённой защитой. | Перейти к 1.5: спроектировать и проверить минимальный LangGraph workflow с checkpoint, лимитами, отменой, trace и evidence-gated research branch из ADR-0003. |
| 2026-09-14 | 1.5 — LangGraph, минимальный управляемый workflow | выполнен | Добавлен синтетический LangGraph workflow: `plan → execute → validate → report`, с checkpoint per `thread_id`, audit trace, отменой до выполнения, лимитом шагов и evidence-gate. Research-ветка без evidence trace блокируется до выполнения в соответствии с ADR-0003; внешние инструменты, LLM и клинические данные не используются. | В isolated RP5 env установлен `langgraph==1.2.11`; `unittest`: 4 workflow tests (engineering path, cancel, missing evidence, checkpoint) и 5 существующих GigaChat config tests — всего `9` passed. Compose после проверки: 6 сервисов running. | `InMemorySaver` доказан только как dev/test checkpoint и не переживает перезапуск. Persistent PostgreSQL checkpointer, ограничение времени/затрат и реальная отмена внешней операции пока не реализованы. | Перейти к 1.6: описать и проверить test-only MCP contracts с allowlist, schema validation и injection-negative tests; до этого не подключать произвольные MCP-server или внешние search tools. |
| 2026-09-14 | 0/1.1 — эксплуатация RP5: логи и резервное копирование | выполнен | Проверены и приняты изменения control plane: Docker JSON-логи всех сервисов ограничены; конфиг host logrotate установлен; ежедневный PostgreSQL backup выполняется из cron с retention. Backup-скрипт исправлен: `.env` больше не исполняется, а читаются только требуемые имена БД как данные. | `docker compose config -q`; inspect всех 6 контейнеров подтвердил `json-file` и лимиты `5m/10m × 3`; system `logrotate -d` успешно; backup-каталог `bimo:bimo 750`, существующий сжатый dump; ручной запуск исправленного скрипта и проверка Compose выполняются перед commit. | Первоначальный скрипт использовал `source .env`, что могло исполнить содержимое env-файла. Это заменено безопасным parser; скрипт не создаёт backup-directory и честно отказывает при отсутствии прав, чтобы не получить `root:root` при ошибочном запуске через sudo. | Продолжить 1.6 MCP only после публикации и проверки сохранённого operational change-set. |
| 2026-09-14 | 1.6.1–1.6.2 — MCP policy и локальный stdio | выполнен | Добавлены три строго allowlisted test-only контракта и локальный `stdio` server на официальном Python MCP SDK 2.2.0. Он объявляет только `test.docs.lookup`, `test.fixture.read`, `test.git.diff`, применяет schema/path-policy до handler и возвращает исключительно synthetic недоверенные данные. Нет обращения к `.env`, сети, файловой системе или Git. | На RP5: `python -m unittest -q tests.test_gigachat_config tests.test_workflow tests.test_mcp_policy tests.test_mcp_stdio` — `14` passed. End-to-end test поднимает отдельный дочерний MCP-процесс, сверяет объявленные инструменты и `trusted=false` в результате. | Первый E2E запуск выявил различие API MCP SDK v2: поле результата называется `is_error`, а не `isError`; тест исправлен и повторный полный набор зелёный. | Добавить transport negative-tests, затем отдельно спроектировать и проверить sandbox handler только для fixture-репозитория; не подключать внешние MCP, shell, сеть или реальные данные. |
| 2026-09-14 | 1.6.3 — MCP transport negative-tests | выполнен | Реальный `stdio` server переведён с высокоуровневого decorator на низкоуровневый официальный интерфейс MCP SDK, чтобы передавать в policy исходный объект аргументов и публиковать `additionalProperties=false`. Предусмотренные отказы возвращаются как контролируемый `is_error=true`; handlers по-прежнему только synthetic. | На RP5: `tests.test_mcp_stdio` — `2` passed. Через дочерний процесс подтверждены: нормальный lookup; запрет `fixtures/../.env`; запрет лишнего `url` у lookup; нет Python traceback при policy-отказе. | Первый negative-run показал два дефекта: обычное `McpPolicyError` логировалось как crash, а high-level SDK v2 молча отбрасывал лишний аргумент. Перехвачена первая причина; для второй выбран низкоуровневый SDK callback с точной JSON Schema и raw validation. | Спроектировать и проверить отдельный sandbox handler только для заранее созданного fixture-репозитория; до review не подключать реальные файлы, Git, shell, сеть или внешние MCP. |
| 2026-09-14 | 1.6.4 — Docker sandbox для synthetic fixture | выполнен | Создан отдельный MCP handler для чтения единственной версионируемой synthetic fixture. Он запускается только opt-in в одноразовом container с `--network none`, read-only filesystem, `no-new-privileges`, непривилегированным UID, без capabilities и с ограниченным tmpfs; монтированы read-only лишь исходники, isolated Python environment и `tests/fixtures/mcp-repository`. | На RP5: `RUN_MCP_DOCKER_EVAL=1 python -m unittest -q tests.test_mcp_docker_fixture` — `1` passed. Дочерний container по stdio объявил один tool, прочитал разрешённую fixture и вернул `trusted=false`; traversal `fixtures/../.env` получил `is_error=true`. Основной Compose не менялся. | Первый запуск не нашёл fixture, потому что каталог на Pi был создан после SCP. Каталог создан до повторной копии; повторный контейнерный E2E тест зелёный. Container использует уже имеющийся ARM64 eval image и read-only isolated venv, поэтому новый image не собирался и сеть не открывалась. | Провести независимый design/review Git-diff fixture и подтверждаемых resource limits на RP5; до этого не подключать рабочий репозиторий, shell, сеть или внешние MCP. |
| 2026-09-14 | 1.6.5 — Docker sandbox для synthetic Git diff | выполнен | Добавлен MCP handler, выполняющий фиксированный `git diff` только над одноразовым synthetic Git fixture. В контейнер read-only монтируются fixture, исходники, isolated venv и только Git binary/git-core; рабочая копия НейроЛаба, `.env`, Docker socket и сеть не монтируются. Вызов разрешён лишь для `base=HEAD` и allowlisted пути; `safe.directory` задан исключительно для `/repository` внутри контейнера. | На RP5: `RUN_MCP_DOCKER_EVAL=1 python -m unittest -q tests.test_mcp_docker_git_fixture` — `1` passed. Подтверждён фактический Git diff одной synthetic правки, `trusted=false` и policy-отказ для `HEAD~1`. Container стартовал с `--pids-limit 64`; `docker info` подтвердил pids support и отсутствие memory limit. Основной Compose не менялся. | Непривилегированный UID не мог читать временный каталог `0700`; тест открывает только synthetic temp root до `0755`. Затем Git отказал из-за ownership; добавлен точный `safe.directory=/repository`, без host config. Evaluation image не содержит Git; бинарник и git-core смонтированы read-only, новый image и сеть не использовались. | Собрать доказанные test-only узлы в полностью synthetic LangGraph trace; внешний поиск, Hermes live-search, OpenHands и рабочий репозиторий пока не включать в автозапуск. |
| 2026-09-14 | 1.6.6 — LangGraph trace над недоверенными MCP-событиями | выполнен | Добавлен отдельный deterministic LangGraph trace для трёх test-only MCP контрактов. Он допускает ровно allowlisted набор событий только после повторной проверки аргументов, `trusted=false` и `origin=test-mcp`; content не участвует в управлении и не попадает в report. Шаг `execute` маркирован как synthetic-only. | На RP5: `python -m unittest -q tests.test_synthetic_mcp_trace` — `4` passed. Подтверждены: нормальный trace; инъекция не появляется в report; неизвестный tool блокируется до execute; поддельный trusted-result блокируется до execute. | Trace сознательно получает event records, а не заново открывает MCP transport: реальные stdio/Docker transport и sandbox уже покрыты отдельными E2E тестами. Checkpoint остаётся `InMemorySaver` только для dev/test. | Собрать воспроизводимый 1.7 smoke runner с единым synthetic отчётом поверх существующих проверок; не включать внешний поиск, Hermes live-search, OpenHands или рабочий репозиторий. |

## 7. Правило обновления

После существенного результата обновить статус подэтапа и журнал: что сделано,
где артефакты, какие проверки действительно прошли, известные ограничения,
проблемы/решения и ближайший безопасный шаг. Не отмечать этап завершённым без
проверяемых доказательств или ясного объяснения, почему проверка невозможна.
